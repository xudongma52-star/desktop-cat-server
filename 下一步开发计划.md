# 下一步开发计划

## 当前

“每日情绪站”的最小可执行闭环已经完成：

- 桌面小猫提供一个简单文本框，用户输入一句话后即可一键发送。
- 不要求选择心情、类型或其他选项；保存成功只做简短确认，不立即生成 AI 回复。
- Electron 通过 Java 接口保存数据，不直接连接数据库。
- 情绪碎片单独存储，不作为文章、日记或长期记忆。
- Java 自动记录日期和时间，提供新增及按天查询能力。
- 网站提供简单的“当天内心”页面，按时间展示当天说过的话。
- 发送失败时保留输入内容，避免丢失尚未保存的情绪。

当前结果：从桌面小猫发送一句话后，Java 会写入数据库，并能在网站“今天的内心”页面看到；真实 PostgreSQL 链路已经验证。

## 以后

- 让大模型读取当天的情绪变化，在合适的时间生成简短、自然的关心话语；不在每次输入后立即回复套话。
- 在网站“当天内心”旁展示小猫对当天状态的分析，但不进行心理诊断。
- 为个人文章实现带来源引用的 RAG 问答，只检索明确允许 AI 使用的内容。
- 实现待办事项和桌面小猫提醒，包括完成、稍后提醒及离线恢复。
- 增加图片回忆、喜欢的话、动漫角色和更多桌面陪伴表现。
- 长期记忆、长期情绪趋势和完整 LLM 对话在积累真实使用数据后再设计，不默认把每次聊天写入长期记忆。
