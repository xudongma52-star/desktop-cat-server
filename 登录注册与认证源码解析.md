# 登录注册与认证源码解析

本文只解释当前仓库中已经落地的认证代码，范围包括 Web 注册登录、Redis Session、登录记忆、CSRF、Electron 桌面端 PKCE 授权、设备免登录和退出登录。不把邀请制、多租户、第三方登录等尚未实现的蓝图能力描述成现状。

## 1. 先看整体：系统里不只有一种“登录凭证”

当前认证体系把不同生命周期的状态分开保存：

| 状态或凭证 | 谁持有 | 服务端保存位置 | 有效期 | 用途 |
| --- | --- | --- | --- | --- |
| 用户密码 | 用户输入 | PostgreSQL 只保存 BCrypt 哈希 | 长期 | 首次注册、Web 密码登录 |
| `JSESSIONID` | 浏览器 Cookie | Redis 中的 Spring Session | 空闲 12 小时 | Web 日常请求认证 |
| `DESKTOP_CAT_REMEMBER_ME` | 浏览器 HttpOnly Cookie | 服务端不保存对应 Token；通过稳定密钥验签 | 30 天 | Session 不存在时恢复 Web 身份 |
| `XSRF-TOKEN` | 浏览器 Cookie | 不作为登录凭证 | 随浏览器会话 | 防止第三方页面伪造 Web 写请求 |
| PKCE 授权码 | Electron 临时接收 | Redis 以授权码 SHA-256 摘要为键 | 2 分钟、只能消费一次 | 把浏览器中的 Web 身份安全交给桌面端 |
| 桌面设备凭证 | Electron `safeStorage` 加密保存 | PostgreSQL 只保存凭证 secret 的 SHA-256 摘要 | 90 天 | 桌面端静默换取短期 Access Token |
| `dcat_...` Access Token | Electron 进程内存 | Redis 以 Token SHA-256 摘要为键 | 15 分钟 | 桌面端调用业务 API |

最重要的边界是：数据库和 Redis 都不保存用户密码、设备 secret、授权码或 Access Token 的明文。浏览器与桌面端也不会互相传递用户密码。

## 2. 代码模块与职责

### 2.1 Web 前端

- [`LoginView.vue`](./apps/web/src/views/LoginView.vue)：收集用户名和密码，做最基础的空值校验。
- [`RegisterView.vue`](./apps/web/src/views/RegisterView.vue)：校验用户名长度、密码字符数、密码 UTF-8 字节数和两次密码是否一致。
- [`auth.ts`](./apps/web/src/api/auth.ts)：封装注册、登录、退出和查询认证状态的 API。
- [`http.ts`](./apps/web/src/api/http.ts)：统一携带 Cookie、补充 CSRF 请求头、设置超时并转换错误响应。
- [`stores/auth.ts`](./apps/web/src/stores/auth.ts)：在 Pinia 中保存当前用户，应用启动时恢复登录状态。
- [`router/index.ts`](./apps/web/src/router/index.ts)：路由进入前等待认证初始化，保护需要登录的页面。
- [`DesktopConnectView.vue`](./apps/web/src/views/DesktopConnectView.vue)：复用浏览器当前账号，为 Electron 发起设备授权。

### 2.2 Java 后端

- [`SecurityConfig.java`](./services/server/src/main/java/com/desktopcat/server/identity/security/SecurityConfig.java)：定义公开接口、受保护接口、认证过滤器、CSRF、Session、Remember-Me 和退出行为。
- [`AuthController.java`](./services/server/src/main/java/com/desktopcat/server/identity/controller/AuthController.java)：Web 注册、登录和状态查询入口。
- [`AuthApplicationService.java`](./services/server/src/main/java/com/desktopcat/server/identity/application/AuthApplicationService.java)：注册规则、密码哈希和用户状态查询。
- [`AppUserDetailsService.java`](./services/server/src/main/java/com/desktopcat/server/identity/security/AppUserDetailsService.java)：把数据库用户转换为 Spring Security 的 `UserDetails`。
- [`DesktopAuthController.java`](./services/server/src/main/java/com/desktopcat/server/identity/controller/DesktopAuthController.java)：桌面授权、换 Token、刷新和撤销入口。
- [`DesktopAuthApplicationService.java`](./services/server/src/main/java/com/desktopcat/server/identity/application/DesktopAuthApplicationService.java)：PKCE、设备凭证和桌面授权的核心业务逻辑。
- [`DesktopAccessTokenService.java`](./services/server/src/main/java/com/desktopcat/server/identity/security/DesktopAccessTokenService.java)：签发并校验 15 分钟 Access Token。
- [`DesktopBearerTokenFilter.java`](./services/server/src/main/java/com/desktopcat/server/identity/security/DesktopBearerTokenFilter.java)：把桌面 Bearer Token 转换成当前请求的 Spring Security 身份。
- [`RedisDesktopAuthStateStore.java`](./services/server/src/main/java/com/desktopcat/server/identity/security/RedisDesktopAuthStateStore.java)：保存一次性授权码和 Access Token 状态。
- [`GlobalExceptionHandler.java`](./services/server/src/main/java/com/desktopcat/server/web/GlobalExceptionHandler.java)：把后端异常统一转换为前端可识别的错误码。

### 2.3 PostgreSQL 与 Redis

- [`V5__create_app_user.sql`](./services/server/src/main/resources/db/migration/V5__create_app_user.sql)：用户表。
- [`V6__create_auth_device.sql`](./services/server/src/main/resources/db/migration/V6__create_auth_device.sql)：桌面设备授权表。
- [`AppUserDao.xml`](./services/server/src/main/resources/mappers/AppUserDao.xml)：用户查询和创建 SQL。
- [`AuthDeviceDao.xml`](./services/server/src/main/resources/mappers/AuthDeviceDao.xml)：设备创建、刷新时间更新和撤销 SQL。
- [`application-postgres.yml`](./services/server/src/main/resources/application-postgres.yml)：Redis 地址、Spring Session 和 Session TTL。

## 3. Spring Security 总入口

认证入口是 `SecurityConfig.securityFilterChain()`。

### 3.1 哪些接口允许匿名访问

当前公开接口包括：

- `GET /api/auth/status`：允许匿名查询当前状态。
- `GET /api/system/status`、`GET /actuator/health`：运行状态和健康检查。
- `POST /api/auth/register`、`POST /api/auth/login`：注册和登录。
- `POST /api/auth/desktop/exchange`：用一次性授权码换设备凭证。
- `POST /api/auth/desktop/refresh`：用设备凭证换短期 Token。
- `POST /api/auth/desktop/revoke`：撤销设备凭证。

其余 `/api/**` 都要求已认证。Web 请求通常依赖 Session，Electron 请求通常依赖 Bearer Token。

“接口允许匿名调用”不代表它没有内部校验。例如 `desktop/refresh` 必须提交有效设备凭证；把它设成 `permitAll`，只是因为调用者此时还没有 Access Token。

### 3.2 为什么禁用表单登录和 HTTP Basic

`formLogin` 和 `httpBasic` 都被关闭。项目不使用 Spring 默认 HTML 登录页，也不让浏览器反复发送用户名密码，而是由 Vue 调用 JSON API，登录成功后使用 Cookie Session。

### 3.3 两条认证路径如何汇合

不管身份来自 Web Session 还是桌面 Bearer Token，最终都会在 `SecurityContextHolder` 中形成一个 `Authentication`：

```text
浏览器 Cookie → Spring Session → SecurityContext → Authentication

Electron Bearer Token → DesktopBearerTokenFilter → SecurityContext → Authentication
```

后面的 Controller 和业务代码只需要读取 `Principal`，不需要理解请求来自哪个客户端。

## 4. Web 注册流程

完整调用链如下：

```text
RegisterView.vue
  → auth store.register()
  → registerUser()
  → POST /api/auth/register
  → AuthController.register()
  → AuthApplicationService.register()
  → AppUserDao / PostgreSQL app_user
  → AuthController.authenticate()
  → Spring Security AuthenticationManager
  → Redis Session + Remember-Me Cookie
```

### 4.1 前端先做体验性校验

`RegisterView.validate()` 会检查：

- 用户名去掉首尾空格后不能为空。
- 用户名为 3～32 个 Unicode 字符。
- 密码至少 6 个 Unicode 字符。
- 密码不能超过 72 个 UTF-8 字节。
- 两次密码必须一致。

前端校验只用于尽快给用户反馈，不能代替后端校验。调用者可以绕过页面直接请求 API，所以同样的核心规则必须在 Java 中再次执行。

### 4.2 后端规范化与校验

`AuthApplicationService.register()` 首先调用 `normalizeAndValidateUsername()` 和 `validatePassword()`：

- 用户名会 `trim()`，但不会统一转为小写，因此 `MaxCat` 和 `maxcat` 当前是两个不同用户名。
- 字符长度使用 `codePointCount()`，避免把部分 Unicode 字符简单按 UTF-16 单元计算。
- 密码额外检查 UTF-8 字节数不超过 72，因为 BCrypt 只可靠处理前 72 字节。

### 4.3 密码如何保存

`PasswordEncodingConfig` 使用：

```java
PasswordEncoderFactories.createDelegatingPasswordEncoder()
```

当前默认编码器是 BCrypt，数据库中的结果带有 `{bcrypt}` 前缀。登录时 Spring Security 会根据前缀选择匹配的编码器，所以未来迁移密码算法时可以兼容旧哈希。

数据库的 `password_hash` 字段只保存哈希，不保存明文密码。日志也只记录新用户的 `userId`，不记录用户名和密码。

### 4.4 为什么既查询重复用户名，又保留数据库唯一约束

Service 会先调用 `selectByUsername()`，这样大多数重复注册可以得到明确提示。但“查询不存在”和“插入”之间可能同时出现另一个注册请求，因此 `app_user.username` 仍有唯一约束。

如果并发插入触发 `DuplicateKeyException`，Service 仍返回 `Username already exists.`。这形成两层保护：

1. 应用层提供友好错误。
2. 数据库层保证最终一致性。

### 4.5 注册成功为什么会自动登录

`AuthController.register()` 创建用户后，没有要求前端再请求一次登录，而是把刚才的用户名和密码交给同一个 `authenticate()` 方法。

因此注册和登录最终共用完全相同的身份验证、Session 保存和 Remember-Me 签发逻辑，避免出现“注册成功但登录方式不同”的分叉。

## 5. Web 密码登录流程

### 5.1 AuthenticationManager 校验链

`AuthController.authenticate()` 创建一个未认证的 `UsernamePasswordAuthenticationToken`，交给 `AuthenticationManager`：

```text
AuthenticationManager
  → DaoAuthenticationProvider
  → AppUserDetailsService.loadUserByUsername()
  → AppUserDao.selectByUsername()
  → PasswordEncoder.matches(明文密码, 数据库哈希)
```

`AppUserDetailsService` 还会读取 `app_user.status`：

- `ACTIVE`：允许登录，并授予 `ROLE_USER`。
- `DISABLED`：构造 disabled 用户，认证失败。

### 5.2 为什么错误用户名和错误密码返回同一提示

`AuthenticationException` 最终统一转换为：

```text
HTTP 401
code = INVALID_CREDENTIALS
message = Invalid username or password.
```

这样不会通过错误信息向外部暴露“这个用户名是否存在”。Web 再把 `INVALID_CREDENTIALS` 映射成中文提示。

### 5.3 登录成功后保存了什么

认证成功后，Controller 显式完成四件事：

1. 创建 `SecurityContext`。
2. 把认证成功的 `Authentication` 放入 Context。
3. 通过 `HttpSessionSecurityContextRepository.saveContext()` 保存 Context。
4. 调用 `RememberMeServices.loginSuccess()` 签发登录记忆 Cookie。

这里使用 `requireExplicitSave(true)`，所以不能只把身份放入线程上下文；Controller 必须显式保存，否则下一个请求不会继续登录。

### 5.4 Session 如何进入 Redis

项目引入 `spring-session-data-redis`，并配置：

```yaml
spring:
  session:
    store-type: redis
    timeout: 12h
    redis:
      namespace: desktop-cat:session
```

Spring Session 会替换 Servlet 容器原本的内存 Session。`HttpSessionSecurityContextRepository` 仍然操作标准 `HttpSession`，但底层实际读写 Redis。

会话 Cookie 名在 `application.yml` 中显式固定为 `JSESSIONID`。这是必要的，因为 Spring Session 自己的默认 Cookie 名是 `SESSION`；显式固定名称可以保持引入 Redis 前后的 Cookie 行为一致，也与退出登录中的 Cookie 清理名称一致。

Session 空闲 12 小时后过期。每次有效请求会续期对应 Redis Session，因此这是空闲超时，不是登录后的绝对 12 小时。

## 6. 登录记忆：为什么下次打开网页不用再次输入密码

`RememberMeConfig` 使用 `TokenBasedRememberMeServices`：

- Cookie 名：`DESKTOP_CAT_REMEMBER_ME`。
- 有效期：30 天。
- `alwaysRemember = true`：每次成功登录都会签发，不需要页面额外勾选“记住我”。
- 生产环境是否添加 `Secure` 属性由 `AUTH_SECURE_COOKIES` 控制。
- 签名密钥由 `AUTH_REMEMBER_ME_KEY` 提供。

浏览器重新打开时会发生两种情况：

1. `JSESSIONID` 对应的 Redis Session 仍存在，直接恢复 `SecurityContext`。
2. Session 已过期或浏览器会话 Cookie 已消失，但 30 天 Remember-Me Cookie 仍有效，Spring Security 重新加载用户并恢复身份。

Remember-Me 不是把密码保存到浏览器。Cookie 的可信性来自服务端密钥、用户信息和有效期的签名校验。

正式部署必须提供稳定、随机的 `AUTH_REMEMBER_ME_KEY`。如果每次部署都换密钥，旧 Cookie 无法通过验签，所有用户会被要求重新登录。

## 7. 页面启动时如何恢复登录状态

`router.beforeEach()` 在进入任何路由前调用 `auth.initialize()`。Pinia Store 只会发起一次初始化请求，并复用同一个 `initializePromise`，避免多个路由并发触发重复状态查询。

初始化调用：

```text
GET /api/auth/status
```

后端从 `Principal` 读取用户名，再查询 `app_user` 返回：

```json
{
  "authenticated": true,
  "userId": 1,
  "username": "MaxCat"
}
```

若没有身份，则返回 `authenticated: false`。前端据此执行路由规则：

- `requiresAuth` 页面未登录：跳转 `/login?redirect=原路径`。
- `guestOnly` 页面已登录：跳转首页。
- 登录或注册成功：只接受以单个 `/` 开头的站内重定向，防止开放重定向到外部网站。

Pinia 只缓存当前用户展示状态，不把密码、Session ID 或 Remember-Me Token 写入 `localStorage`。

## 8. CSRF 为什么存在，以及前后端怎样配合

Web 身份由 Cookie 自动携带。如果完全不做 CSRF 防护，恶意网站可能诱导浏览器向本项目发送带 Cookie 的写请求。

后端使用 `CookieCsrfTokenRepository`：

- Cookie 名使用 Spring 默认的 `XSRF-TOKEN`。
- Cookie 对 JavaScript 可读，因为前端需要把值复制到请求头。
- Cookie 路径为 `/`，`SameSite=Lax`。
- `CsrfCookieFilter` 主动读取延迟生成的 Token，确保响应把 Cookie 写给浏览器。

`apiRequest()` 对 `POST`、`PUT`、`PATCH`、`DELETE` 等非安全方法执行：

```text
读取 XSRF-TOKEN Cookie
  → 写入 X-XSRF-TOKEN 请求头
  → fetch(..., credentials: 'include')
```

因此攻击者即使能诱导浏览器携带认证 Cookie，也无法读取本项目的 CSRF Cookie并构造正确请求头。

桌面端的 Bearer 请求不依赖浏览器 Cookie，所以带 `Authorization: Bearer ...` 的请求忽略 CSRF。`desktop/exchange`、`refresh`、`revoke` 也忽略 CSRF，但分别依赖 PKCE 授权码或高强度设备凭证。

## 9. Electron 首次连接：PKCE 完整流程

桌面端不能安全内置一个所有安装包都共用的固定客户端密钥。因此当前实现使用系统浏览器、回环地址和 PKCE，把已有 Web 身份交给 Electron。

### 9.1 Electron 生成三项临时数据

`openBrowserAuthorization()` 生成：

- `codeVerifier`：32 个安全随机字节，Base64URL 编码。
- `codeChallenge`：`BASE64URL(SHA-256(codeVerifier))`。
- `state`：24 个安全随机字节，防止回调被串换。

Electron 同时在 `127.0.0.1` 随机端口启动一次性 HTTP 回调服务器，回调地址形如：

```text
http://127.0.0.1:49152/callback
```

随后用系统默认浏览器打开：

```text
/desktop/connect
  ?redirect_uri=...
  &code_challenge=...
  &state=...
  &device_name=...
  &platform=...
  &app_version=...
```

### 9.2 浏览器复用当前 Web 登录

`/desktop/connect` 路由带有 `requiresAuth`。如果浏览器已经通过 Session 或 Remember-Me 登录，不需要再次输入密码；否则先去登录页，成功后再返回连接页。

`DesktopConnectView` 调用 `POST /api/auth/desktop/authorize`。这个接口不是匿名接口，后端从 `Principal` 获取当前 Web 用户，而不是相信请求体传来的用户 ID。

### 9.3 后端严格校验回环地址

`DesktopAuthApplicationService.validateRedirectUri()` 要求：

- 协议只能是 `http`。
- 主机必须精确为 `127.0.0.1`，不接受外部域名。
- 端口必须在 1024～65535。
- 路径必须精确为 `/callback`。
- 不允许原始 query、fragment 或 userInfo。

后端还会校验 PKCE challenge、state、设备名、平台和版本的格式与长度。

### 9.4 两分钟授权码如何保存

校验通过后，后端生成 32 个安全随机字节作为授权码。明文只进入回调 URL；Redis 的键使用授权码 SHA-256 摘要：

```text
desktop-cat:auth:authorization-code:<codeHash>
```

Redis value 保存用户 ID、回调地址、PKCE challenge 和设备元数据，TTL 为 2 分钟。

`RedisDesktopAuthStateStore.takeAuthorization()` 使用 Redis `GETDEL` 语义读取并删除，所以授权码只能消费一次。当前实现会先消费，再校验 verifier；即使有人提交错误 verifier，该授权码也会立即失效，不能反复尝试。

### 9.5 Electron 校验 state，再提交 verifier

浏览器跳回 Electron 的回环服务器时携带 `code` 和 `state`。Electron 先比较返回的 `state` 是否与本地随机值完全相同，再调用：

```text
POST /api/auth/desktop/exchange
```

请求包含授权码、只保存在 Electron 内存里的 `codeVerifier` 和原始回调地址。

后端重新计算 `BASE64URL(SHA-256(verifier))`，并使用常量时间比较与 Redis 中的 challenge 对照，同时要求回调地址完全一致。

## 10. 设备凭证与短期 Access Token

### 10.1 首次交换成功后的返回值

PKCE 校验成功后，服务端生成：

- `deviceId`：随机 UUID。
- `secret`：32 个安全随机字节。
- `deviceCredential`：`deviceId.secret`。
- Access Token：`dcat_` 加 32 个安全随机字节。

设备凭证明文只返回这一次。PostgreSQL `auth_device` 保存：

- `device_id`。
- `user_id`。
- `SHA-256(secret)`。
- 设备名、平台、版本。
- 90 天过期时间。
- 最近使用时间和撤销时间。

Electron 使用 `safeStorage.encryptString()` 加密完整设备凭证，再写入用户目录下的 `auth-state.json`。Access Token 不落盘，只保存在主进程内存。

### 10.2 Access Token 如何保存和验证

`DesktopAccessTokenService.issue()` 生成 `dcat_...` Token，但 Redis 键只使用 SHA-256 摘要：

```text
desktop-cat:auth:access-token:<tokenHash>
```

value 保存用户 ID、用户名和明确过期时间，Redis TTL 为 15 分钟。

Electron 请求业务 API 时设置：

```http
Authorization: Bearer dcat_...
```

`DesktopBearerTokenFilter` 在 Spring Security 用户名密码过滤器之前运行：

1. 当前请求还没有身份时才尝试 Bearer 认证。
2. 检查 `Bearer ` 前缀。
3. 对 Token 做 SHA-256，从 Redis 查询身份。
4. 再检查 value 中的绝对过期时间。
5. 构造带 `ROLE_USER` 的 `Authentication` 放入当前请求上下文。

桌面 Bearer 身份只服务当前请求，不创建 Web Session。

## 11. 桌面端重启后为什么可以静默登录

Electron 启动后执行 `loadDeviceCredential()`：

1. 读取 `auth-state.json`。
2. 使用当前操作系统用户的 `safeStorage` 解密。
3. 恢复内存中的 `deviceCredential`。

需要请求后端时，`ensureAccessToken()` 按以下顺序工作：

```text
内存 Access Token 尚有 30 秒以上有效期
  → 直接复用

否则存在设备凭证
  → POST /api/auth/desktop/refresh
  → 换取新的 15 分钟 Access Token

否则且允许交互
  → 打开系统浏览器走 PKCE
```

刷新时后端会依次验证：

- `deviceId.secret` 格式正确。
- `auth_device` 记录存在。
- `revoked_at` 为空。
- 90 天有效期未结束。
- `SHA-256(secret)` 与数据库摘要常量时间相等。
- 所属用户仍为 `ACTIVE`。
- 条件更新 `last_used_at` 成功。

刷新响应不再返回设备凭证，只返回新的短期 Access Token。桌面端因此不需要频繁改写安全存储。

`authenticatedFetch()` 遇到业务请求返回 401 时，会清掉内存 Access Token、刷新一次并重试原请求。并发认证通过 `authenticationInFlight` 合并，避免多个 API 同时触发多次刷新。

## 12. 退出登录与撤销设备

### 12.1 Web 退出

前端调用 `POST /api/auth/logout`，并正常携带 CSRF 请求头。Spring Security 随后：

- 失效当前 Session，Redis 中对应 Session 会被删除。
- 清理 `JSESSIONID`。
- Remember-Me 服务参与退出流程并清除 `DESKTOP_CAT_REMEMBER_ME`。
- 返回 HTTP 204。

Pinia 收到成功响应后把内存中的 `user` 设为 `null`。

### 12.2 桌面端撤销

Electron 调用 `POST /api/auth/desktop/revoke`，提交设备凭证。后端校验 secret 摘要后，把 `auth_device.revoked_at` 写为当前时间。

撤销成功或服务端返回 401 时，Electron 清理本机加密设备凭证和内存 Token。若是网络故障或服务端异常，则保留本地凭证，避免在没有确认服务端结果时造成不可恢复的本地退出。

当前实现撤销的是长期设备凭证，已经签发的短期 Access Token 没有关联 `deviceId`，最长还可能继续有效 15 分钟。这是现有实现的明确边界。

## 13. 统一错误响应

业务代码主要抛出带 HTTP 状态和明确英文原因的 `ResponseStatusException`。`GlobalExceptionHandler` 把原因映射为稳定错误码，例如：

| 场景 | HTTP | 错误码 |
| --- | ---: | --- |
| 用户名已存在 | 409 | `USERNAME_ALREADY_EXISTS` |
| 用户名或密码错误 | 401 | `INVALID_CREDENTIALS` |
| PKCE challenge 格式错误 | 400 | `PKCE_CHALLENGE_INVALID` |
| 授权码过期或不存在 | 401 | `AUTHORIZATION_CODE_INVALID` |
| 设备凭证无效或过期 | 401 | `DEVICE_CREDENTIAL_INVALID` |
| 未登录访问受保护接口 | 401 | `AUTHENTICATION_REQUIRED` |
| CSRF 或权限校验失败 | 403 | `ACCESS_DENIED` |

每个错误响应还包含 `requestId`、时间和请求路径。Web 的 `ApiError` 保留这些字段，再由 `describeAuthError()` 把稳定错误码转换为用户可读中文，不依赖后端英文文本做页面逻辑。

## 14. Redis 在这一套认证中的具体职责

Redis 只承载短期、可过期、需要跨 Java 进程重启保留的状态：

```text
desktop-cat:session:...                         Web Spring Session，空闲 12 小时
desktop-cat:auth:authorization-code:<hash>      PKCE 授权码，2 分钟
desktop-cat:auth:access-token:<hash>            桌面 Access Token，15 分钟
```

Redis 不保存：

- 用户密码哈希之外的数据库主数据。
- 设备凭证明文。
- 长期设备授权的最终状态。

用户和设备归属仍由 PostgreSQL 负责。这样即使 Redis 数据因故清空，最坏结果是 Web Session 和短期 Token 失效，用户重新登录或桌面端用设备凭证静默刷新，不会丢失账号与设备记录。

当前本地配置连接 `127.0.0.1:6380`，Docker 端口只绑定宿主机回环地址，没有设置 Redis 账号密码。若未来把 Redis 放到服务器或跨机器访问，必须先定义账号密码并补充 TLS/网络隔离配置，不能直接沿用本地无认证方式。

## 15. Cookie 与关键安全配置

[`application.yml`](./services/server/src/main/resources/application.yml) 中的关键配置：

- `server.address=127.0.0.1`：开发服务只监听本机。
- Session Cookie 显式命名为 `JSESSIONID`。
- Session Cookie 为 HttpOnly、SameSite=Lax。
- `AUTH_SECURE_COOKIES` 同时控制 Session 和 Remember-Me Cookie 的 Secure 属性。
- `AUTH_REMEMBER_ME_KEY` 提供 Remember-Me 签名密钥。

本地 HTTP 开发时 `AUTH_SECURE_COOKIES=false`；正式 HTTPS 部署必须设为 `true`。

## 16. 自动化测试覆盖了什么

- [`AuthControllerTest.java`](./services/server/src/test/java/com/desktopcat/server/identity/AuthControllerTest.java)：注册成功、自动登录、重复用户名、参数校验、正确/错误密码、禁用账号和匿名状态。
- [`DesktopAuthApplicationServiceTest.java`](./services/server/src/test/java/com/desktopcat/server/identity/DesktopAuthApplicationServiceTest.java)：PKCE 交换、设备凭证刷新、非法回调地址和错误 verifier。
- [`RedisDesktopAuthStateStoreTest.java`](./services/server/src/test/java/com/desktopcat/server/identity/RedisDesktopAuthStateStoreTest.java)：Redis key、TTL、授权码原子消费、Access Token 查询与删除。

当前 Maven `verify` 共 27 项测试通过。真实运行还验证了：

- Spring Boot 能同时连接 PostgreSQL 和 Docker Redis。
- Actuator 健康状态为 `UP`。
- Electron 重启后能读取既有设备凭证。
- `/api/auth/desktop/refresh` 返回 200。
- Redis 中实际出现带 15 分钟 TTL 的 Access Token 状态。

## 17. 当前实现还没有覆盖的能力

理解源码时必须区分“已登录”和“已完成多租户”：

- 当前认证已经能确认请求是谁，但业务表和多数业务 SQL 尚未按 `user_id` 或 `space_id` 做数据归属过滤。多用户登录不等于多租户隔离已经完成。
- 注册当前是开放注册，还没有邀请码、邮箱验证或管理员审批。
- 没有基于 Redis 的登录失败次数限制、IP 限流或账号临时锁定。
- Web 登录始终签发 Remember-Me，没有“仅本次登录”的用户选项。
- 没有用户可见的设备列表、批量撤销和远程下线页面。
- 设备撤销后，已经签发的 Access Token 最长仍可用 15 分钟。
- 用户名当前区分大小写。
- 本地 Redis 没有账号密码，只适用于回环地址开发环境。

因此下一阶段如果继续完善登录体系，优先顺序应是：登录限流与审计、设备列表及远程撤销、邀请制注册，然后再把可信用户身份接入 `user_id + space_id` 的数据隔离。完整 DDD 可以继续按业务复杂度渐进引入，不需要为了认证模块提前批量制造领域抽象。
