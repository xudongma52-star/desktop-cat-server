# 猫的角落 · 当前接口文档

本文档记录当前已经实现并可调用的 HTTP 接口。开发环境后端地址为 `http://127.0.0.1:8080`。

## 1. 调用约定

| 项目 | 说明 |
| --- | --- |
| 普通接口格式 | 请求体和响应体使用 `application/json` |
| SSE 接口格式 | `text/event-stream` |
| 日期格式 | `recordDate`、情绪查询参数 `date` 使用 `yyyy-MM-dd`，例如 `2026-09-16` |
| 时间格式 | `createdAt`、`updatedAt` 等使用 ISO 8601，例如 `2026-09-16T08:30:00Z` |
| 成功响应 | 直接返回业务对象，不额外包装 `code`、`data` |
| 请求编号 | 每次响应都带 `X-Request-Id`；排查问题时可用它对应后端日志 |
| 鉴权 | Web 使用 Session、Remember-Me Cookie 和 CSRF；桌面端使用短期 Bearer Token |
| 数据库接口 | 小猫资料、个人文章、每日情绪、待办提醒和照片轮播接口只在 `postgres` 配置下启用；它也是当前默认配置 |

客户端也可以主动传入 `X-Request-Id`。值必须为 8～64 位，只能包含字母、数字、点、下划线或短横线；不符合要求时后端会重新生成。

## 2. 接口总览

| 模块 | 方法 | 路径 | 用途 |
| --- | --- | --- | --- |
| 系统 | `GET` | `/api/system/status` | 查询后端运行状态和版本 |
| 系统 | `GET` | `/actuator/health` | 健康检查 |
| 认证 | `GET` | `/api/auth/status` | 查询当前 Web 或 Bearer 登录状态 |
| 认证 | `POST` | `/api/auth/register` | 注册并自动登录 |
| 认证 | `POST` | `/api/auth/login` | 登录并建立记忆 Cookie |
| 认证 | `POST` | `/api/auth/logout` | 退出 Web 登录 |
| 桌面认证 | `POST` | `/api/auth/desktop/authorize` | 为桌面猫签发 PKCE 授权码 |
| 桌面认证 | `POST` | `/api/auth/desktop/exchange` | 用授权码换取设备凭证和 Access Token |
| 桌面认证 | `POST` | `/api/auth/desktop/refresh` | 静默刷新 Access Token |
| 桌面认证 | `POST` | `/api/auth/desktop/revoke` | 撤销本机设备凭证 |
| 小猫资料 | `GET` | `/api/cat/profile` | 查询当前小猫资料 |
| 小猫资料 | `PATCH` | `/api/cat/profile/name` | 修改小猫名字 |
| 个人文章 | `POST` | `/api/records` | 创建文章 |
| 个人文章 | `GET` | `/api/records` | 分页查询文章 |
| 写作足迹 | `GET` | `/api/records/activity` | 按日期聚合每天的文章篇数 |
| 个人文章 | `GET` | `/api/records/{recordId}` | 查询文章详情 |
| 个人文章 | `PUT` | `/api/records/{recordId}` | 修改文章 |
| 个人文章 | `DELETE` | `/api/records/{recordId}` | 逻辑删除文章 |
| 温馨回忆 | `GET` | `/api/records/recalls` | 查询允许轮播的文字回忆 |
| 知识检索 | `POST` | `/api/knowledge/retrieve` | 查询当前用户允许检索的相关原文片段 |
| 每日情绪 | `POST` | `/api/emotions` | 保存一句当天的心情或吐槽 |
| 每日情绪 | `GET` | `/api/emotions` | 按日期查询情绪 |
| 待办提醒 | `POST` | `/api/reminders` | 创建一次性提醒 |
| 待办提醒 | `GET` | `/api/reminders` | 查询今天或全部未完成提醒 |
| 待办提醒 | `PUT` | `/api/reminders/{reminderId}` | 修改未完成提醒 |
| 待办提醒 | `PATCH` | `/api/reminders/{reminderId}/complete` | 完成提醒 |
| 待办提醒 | `DELETE` | `/api/reminders/{reminderId}` | 逻辑删除提醒 |
| 照片轮播 | `POST` | `/api/carousel/photos` | 上传裁剪后的轮播照片 |
| 照片轮播 | `GET` | `/api/carousel/photos` | 查询当前用户的轮播照片 |
| 照片轮播 | `GET` | `/api/carousel/photos/{photoId}/content` | 读取照片内容 |
| 照片轮播 | `DELETE` | `/api/carousel/photos/{photoId}` | 将照片移出轮播 |
| 实时事件 | `GET` | `/api/events` | 订阅小猫资料与提醒变更事件 |

### 2.1 桌面统一登录流程

桌面猫只通过系统浏览器获得用户授权，不接触用户名和密码。`authorize` 需要有效 Web Session 和 CSRF 请求头；`exchange`、`refresh`、`revoke` 不使用浏览器 Cookie。桌面业务请求携带 `Authorization: Bearer dcat_...`。

`POST /api/auth/desktop/authorize` 请求体：

```json
{
  "redirectUri": "http://127.0.0.1:49152/callback",
  "codeChallenge": "base64url-sha256-code-challenge",
  "state": "random-base64url-state",
  "deviceName": "MAX-PC 的电脑",
  "platform": "win32",
  "appVersion": "0.3.0"
}
```

成功返回只能使用一次、两分钟内有效的回调地址。回调地址只允许 `127.0.0.1` 随机端口和 `/callback` 路径。

`POST /api/auth/desktop/exchange` 请求体：

```json
{
  "code": "one-time-code",
  "codeVerifier": "original-pkce-verifier",
  "redirectUri": "http://127.0.0.1:49152/callback"
}
```

成功返回：

```json
{
  "accessToken": "dcat_short_lived_token",
  "accessTokenExpiresAt": "2026-09-19T02:15:00Z",
  "deviceId": "be1dd663-2dc6-4f4c-8ac8-c2238c015615",
  "deviceCredential": "deviceId.randomSecret",
  "userId": 1,
  "username": "MaxCat"
}
```

`deviceCredential` 只在首次交换时返回。刷新和撤销接口的请求体均为：

```json
{
  "deviceCredential": "deviceId.randomSecret"
}
```

刷新成功返回相同结构，但 `deviceCredential` 为 `null`。撤销成功返回 `204 No Content`。

## 3. 系统接口

### 3.1 查询运行状态

`GET /api/system/status`

用于确认 Vue 前端与 Java 后端已经连通，并查看当前运行版本。

```json
{
  "status": "UP",
  "application": "desktop-cat-server",
  "springBootVersion": "3.5.16",
  "javaVersion": "21.0.12",
  "timestamp": "2026-09-16T08:30:00Z"
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `status` | string | 服务状态，正常时为 `UP` |
| `application` | string | 后端应用名称 |
| `springBootVersion` | string | 当前 Spring Boot 版本 |
| `javaVersion` | string | 当前 Java 运行版本 |
| `timestamp` | string | 后端生成响应的时间 |

### 3.2 健康检查

`GET /actuator/health`

用于本地检查或后续服务器健康探测，不返回数据库密码等敏感信息。

```json
{
  "status": "UP"
}
```

## 4. 小猫资料接口

### 4.1 查询当前小猫资料

`GET /api/cat/profile`

```json
{
  "profileId": 1,
  "catName": "小饼干",
  "version": 0,
  "updatedAt": "2026-09-16T08:30:00Z"
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `profileId` | number | 小猫资料主键，修改名字时原样传回 |
| `catName` | string | 当前小猫名字，最多 20 个字符 |
| `version` | number | 乐观锁版本号，修改名字时必须传最新值 |
| `updatedAt` | string | 最近更新时间 |

### 4.2 修改小猫名字

`PATCH /api/cat/profile/name`

请求体：

```json
{
  "profileId": 1,
  "catName": "小饼干",
  "version": 0
}
```

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `profileId` | 是 | 正整数，来自查询资料接口 |
| `catName` | 是 | 去除首尾空格后不能为空，最多 20 个字符 |
| `version` | 是 | 非负整数，必须与数据库当前版本一致 |

成功时返回更新后的完整小猫资料，HTTP 状态码为 `200`，并把 `version` 加 1。随后后端会通过 SSE 发布 `cat-profile.updated` 事件。

常见失败：资料不存在时返回 `404 CAT_PROFILE_NOT_FOUND`；版本过期时返回 `409 CAT_PROFILE_VERSION_CONFLICT`。

## 5. 个人文章接口

### 5.1 文章字段

| 字段 | 类型 | 创建 | 修改 | 说明 |
| --- | --- | --- | --- | --- |
| `recordId` | number | 后端生成 | 路径传入 | 文章主键 |
| `recordType` | string | 必填 | 必填 | `DIARY`、`THOUGHT` 或 `WORK_NOTE`，小写输入也会转为大写 |
| `title` | string/null | 可选 | 可选 | 标题，最多 120 个字符；空白内容按 `null` 保存 |
| `content` | string | 必填 | 必填 | 正文，不能全为空白，最多 100000 个字符 |
| `recordDate` | string | 必填 | 必填 | 文章日期，格式为 `yyyy-MM-dd` |
| `mood` | string/null | 可选 | 可选 | 心情，最多 32 个字符；空白内容按 `null` 保存 |
| `recallEnabled` | boolean | 可选 | 可选 | 是否进入温馨回忆轮播；未传时按 `false` 处理 |
| `ragEnabled` | boolean | 可选 | 可选 | 是否允许正文进入个人知识检索；第一版不建立持久化向量索引 |
| `version` | number | 不传 | 必填 | 乐观锁版本号；修改成功后自动加 1 |
| `createdAt` | string | 后端生成 | 后端维护 | 创建时间 |
| `updatedAt` | string | 后端生成 | 后端维护 | 最近更新时间 |

文章类型：

| 值 | 含义 |
| --- | --- |
| `DIARY` | 日记 |
| `THOUGHT` | 心得、感想 |
| `WORK_NOTE` | 工作或实习笔记 |

### 5.2 创建文章

`POST /api/records`

```json
{
  "recordType": "DIARY",
  "title": "今天的小事",
  "content": "下班回家时，小猫在门口等我。",
  "recordDate": "2026-09-16",
  "mood": "安心",
  "recallEnabled": true,
  "ragEnabled": true
}
```

成功时返回完整文章，HTTP 状态码为 `201`：

```json
{
  "recordId": 1,
  "recordType": "DIARY",
  "title": "今天的小事",
  "content": "下班回家时，小猫在门口等我。",
  "recordDate": "2026-09-16",
  "mood": "安心",
  "recallEnabled": true,
  "ragEnabled": true,
  "version": 0,
  "createdAt": "2026-09-16T08:30:00Z",
  "updatedAt": "2026-09-16T08:30:00Z"
}
```

### 5.3 分页查询文章

`GET /api/records?page=1&pageSize=12&recordType=DIARY`

| 查询参数 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `page` | 否 | `1` | 页码，从 1 开始 |
| `pageSize` | 否 | `12` | 每页数量，范围 1～100 |
| `recordType` | 否 | 全部 | 按文章类型筛选；空字符串也表示全部 |

结果按 `recordDate` 倒序排列，同一天按 `recordId` 倒序排列。逻辑删除的文章不会出现。

```json
{
  "items": [
    {
      "recordId": 1,
      "recordType": "DIARY",
      "title": "今天的小事",
      "excerpt": "下班回家时，小猫在门口等我。",
      "recordDate": "2026-09-16",
      "mood": "安心",
      "recallEnabled": true,
      "ragEnabled": true,
      "version": 0,
      "createdAt": "2026-09-16T08:30:00Z",
      "updatedAt": "2026-09-16T08:30:00Z"
    }
  ],
  "page": 1,
  "pageSize": 12,
  "total": 1,
  "totalPages": 1
}
```

`items` 只返回单行摘要 `excerpt`，不返回完整正文。摘要会合并连续空白，最多保留 160 个字符；查看全文应调用详情接口。

### 5.4 查询文章详情

`GET /api/records/{recordId}`

路径参数 `recordId` 必须为正整数。成功时返回与“创建文章”相同的完整文章结构；文章不存在或已经逻辑删除时返回 `404 PERSONAL_RECORD_NOT_FOUND`。

### 5.5 修改文章

`PUT /api/records/{recordId}`

这是完整更新接口，调用时应提交当前文章的全部可编辑字段。`version` 必须使用最近一次查询得到的值。

```json
{
  "recordType": "THOUGHT",
  "title": "重新整理后的想法",
  "content": "今天把这件事想明白了一点。",
  "recordDate": "2026-09-16",
  "mood": "平静",
  "recallEnabled": true,
  "ragEnabled": true,
  "version": 0
}
```

成功时返回更新后的完整文章，HTTP 状态码为 `200`，`version` 自动加 1。其他客户端已经修改过该文章时返回 `409 PERSONAL_RECORD_VERSION_CONFLICT`，应重新读取详情后再编辑。

### 5.6 删除文章

`DELETE /api/records/{recordId}?version=0`

| 参数 | 位置 | 必填 | 说明 |
| --- | --- | --- | --- |
| `recordId` | 路径 | 是 | 要删除的文章主键，必须为正整数 |
| `version` | 查询参数 | 是 | 最近一次读取到的非负版本号 |

成功时返回 `204 No Content`，没有响应体。该操作为逻辑删除，数据仍保留在数据库中，但不会再被列表、详情和回忆接口读取。版本过期时返回 `409 PERSONAL_RECORD_VERSION_CONFLICT`。

### 5.7 查询温馨回忆

`GET /api/records/recalls?limit=10`

只返回未删除且 `recallEnabled=true` 的文章，供首页文字轮播使用。

| 查询参数 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `limit` | 否 | `10` | 返回数量，范围 1～50 |

```json
[
  {
    "recordId": 1,
    "recordType": "DIARY",
    "title": "今天的小事",
    "excerpt": "下班回家时，小猫在门口等我。",
    "mood": "安心",
    "recordDate": "2026-09-16"
  }
]
```

该接口只返回轮播需要的字段和最多 160 个字符的摘要，不返回全文、RAG 开关和版本号。

### 5.8 查询写作足迹

`GET /api/records/activity?startDate=2025-09-18&endDate=2026-09-17&recordType=DIARY`

该接口按 `recordDate` 聚合指定范围内的文章篇数。首页固定查询最近 365 天的日记，用于绘制 53 周写作热力图。逻辑删除的文章不会参与统计，响应中不包含标题或正文。

| 查询参数 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `startDate` | 是 | 无 | 开始日期，格式为 `yyyy-MM-dd`，包含当天 |
| `endDate` | 是 | 无 | 结束日期，格式为 `yyyy-MM-dd`，包含当天 |
| `recordType` | 否 | `DIARY` | `DIARY`、`THOUGHT` 或 `WORK_NOTE` |

开始日期不能晚于结束日期，包含首尾的查询范围最多为 366 天。

```json
{
  "startDate": "2025-09-18",
  "endDate": "2026-09-17",
  "totalRecords": 3,
  "activeDays": 2,
  "days": [
    {
      "recordDate": "2026-09-16",
      "recordCount": 1
    },
    {
      "recordDate": "2026-09-17",
      "recordCount": 2
    }
  ]
}
```

`days` 只返回至少有一篇文章的日期。没有写作记录的日期由前端补为空白格子。

### 5.9 检索个人知识

`POST /api/knowledge/retrieve`

该接口需要登录。Java 只读取当前用户最近 100 篇记录，并把其中 `ragEnabled=true` 的正文发送给本机 Python 服务；单篇正文最多检索前 30000 个字符。Python 不连接数据库，使用字符级 TF-IDF 返回最多 5 个相关片段。配置火山方舟后，Python 会把问题和最多 8000 个字符的命中片段发送给豆包，并要求模型只根据片段生成带 `[1]`、`[2]` 来源编号的回答；片段中的文字只作为资料，不作为模型指令执行。

请求体：

```json
{
  "question": "我以前压力大时是怎么放松的？"
}
```

`question` 去除首尾空格后不能为空，最多 500 个字符。

成功返回：

```json
{
  "searchableRecordCount": 2,
  "candidateLimitReached": false,
  "answer": "你以前会在晚上出去散步，让压力慢慢减轻。[1]",
  "answerGenerated": true,
  "matches": [
    {
      "recordId": 12,
      "recordType": "DIARY",
      "title": "实习第三周",
      "content": "晚上出去散步以后，我的压力减轻了很多。",
      "recordDate": "2026-09-22",
      "score": 0.7214
    }
  ]
}
```

| 字段 | 说明 |
| --- | --- |
| `searchableRecordCount` | 本次实际送入检索的记录数量 |
| `candidateLimitReached` | 是否还有最近 100 篇之外的记录未参与第一版检索 |
| `answer` | 豆包基于命中片段生成的回答；未生成时为 `null` |
| `answerGenerated` | 本次是否成功生成大模型回答 |
| `matches` | 按相关度从高到低排列的原文片段 |
| `score` | 0～1 的余弦相似度，只用于本次结果的相对比较 |

没有开启检索的记录时返回空 `matches`，不会调用 Python。火山方舟未配置、超时或响应异常时，接口仍返回检索片段，并将 `answer=null`、`answerGenerated=false`。Python 服务未启动或响应异常时返回 `503 KNOWLEDGE_RETRIEVAL_SERVICE_UNAVAILABLE`。

## 6. 每日情绪接口

每日情绪用于保存随手说给桌面小猫的一句话，与个人文章分开存储。当前不要求情绪分类，也不会在保存后立即调用 AI。

### 6.1 保存一条情绪

`POST /api/emotions`

```json
{
  "content": "今天有点累，但终于把这件事做完了。"
}
```

`content` 必填，不能全为空白。创建请求不接收记录日期；即使客户端额外提交 `recordDate`，也不会改变保存日期。后端会去掉首尾空白，按照 `Asia/Shanghai` 时区的当天自动确定 `recordDate`，因此不能补写过去或预写明天，并生成创建时间。成功时返回 `201`：

```json
{
  "emotionId": 1,
  "content": "今天有点累，但终于把这件事做完了。",
  "recordDate": "2026-09-16",
  "createdAt": "2026-09-16T12:30:00Z"
}
```

### 6.2 按日期查询情绪

`GET /api/emotions?date=2026-09-16`

网站使用该接口回看今天或过去日期的内容，日期导航不会提供未来日期入口。该查询能力只负责读取，不影响创建情绪时由服务端确定的当天日期。

| 查询参数 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `date` | 否 | 上海时区的今天 | 要查看的日期，格式为 `yyyy-MM-dd` |

结果按照创建时间升序排列，同一时刻按照 `emotionId` 升序排列。当天没有内容时返回空数组：

```json
[
  {
    "emotionId": 1,
    "content": "今天有点累，但终于把这件事做完了。",
    "recordDate": "2026-09-16",
    "createdAt": "2026-09-16T12:30:00Z"
  }
]
```

## 7. 待办提醒接口

当前实现为一次性提醒。网站负责创建和管理，桌面猫定期读取未完成提醒并在到期时出现。重复规则、提前提醒和跨设备认领尚未加入当前数据模型。

### 7.1 提醒字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `reminderId` | number | 提醒主键，由后端生成 |
| `content` | string | 提醒内容，去除首尾空格后不能为空，最多 200 个字符 |
| `remindAt` | string | 触发时间，使用带时区的 ISO 8601 时间 |
| `status` | string | `PENDING` 或 `COMPLETED` |
| `completedAt` | string/null | 完成时间，未完成时为 `null` |
| `version` | number | 乐观锁版本号，修改、完成和删除时必须传最新值 |
| `createdAt` | string | 创建时间 |
| `updatedAt` | string | 最近更新时间 |

### 7.2 创建提醒

`POST /api/reminders`

```json
{
  "content": "站起来活动一下",
  "remindAt": "2026-09-17T10:30:00+08:00"
}
```

`content` 和 `remindAt` 都是必填项，`remindAt` 不能早于后端收到请求时的当前时间。创建成功返回 `201 Created` 和完整提醒：

```json
{
  "reminderId": 1,
  "content": "站起来活动一下",
  "remindAt": "2026-09-17T02:30:00Z",
  "status": "PENDING",
  "completedAt": null,
  "version": 0,
  "createdAt": "2026-09-17T01:00:00Z",
  "updatedAt": "2026-09-17T01:00:00Z"
}
```

### 7.3 查询提醒

`GET /api/reminders?scope=TODAY`

| 查询参数 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `scope` | 否 | `TODAY` | `TODAY` 返回上海时区今天触发的有效提醒，包含已完成项；`PENDING` 返回全部未完成提醒 |

结果按提醒时间、提醒主键升序排列，没有数据时返回空数组。`scope` 不区分大小写，其他值返回 `400 REMINDER_SCOPE_INVALID`。

### 7.4 修改提醒

`PUT /api/reminders/{reminderId}`

```json
{
  "content": "站起来活动十分钟",
  "remindAt": "2026-09-17T11:00:00+08:00",
  "version": 0
}
```

该接口完整更新内容和时间，只能修改 `PENDING` 状态的提醒，新的 `remindAt` 也不能早于当前时间。成功时返回更新后的完整对象，`version` 自动加 1。版本过期时返回 `409 REMINDER_VERSION_CONFLICT`。

### 7.5 完成提醒

`PATCH /api/reminders/{reminderId}/complete`

```json
{
  "version": 1
}
```

成功时返回完整对象，`status` 变为 `COMPLETED`，写入 `completedAt`，`version` 自动加 1。网页和桌面猫的“完成”操作都调用此接口。

### 7.6 删除提醒

`DELETE /api/reminders/{reminderId}?version=1`

成功时返回 `204 No Content`。该操作写入 `deleted_at`，后续查询不再返回该提醒；数据库记录仍保留。版本过期时返回 `409 REMINDER_VERSION_CONFLICT`。

## 8. 照片轮播

所有照片接口都需要登录。浏览器先在本地把用户选择的 JPG、PNG 或 WebP 原图裁成 4:3，并导出为 `1200 × 900` WebP；服务器只保存这一份成品，不保存原图。每张未删除的照片都会自动参加首页轮播。

### 8.1 上传照片

`POST /api/carousel/photos`

请求使用 `multipart/form-data`，文件字段名为 `file`。成品必须是 `image/webp`、尺寸必须为 `1200 × 900`，且不能超过 2 MB。成功返回 `201 Created`：

```json
{
  "photoId": 1,
  "contentUrl": "/api/carousel/photos/1/content",
  "createdAt": "2026-09-19T02:30:00Z"
}
```

数据库只保存用户、文件相对路径和上传时间。文件默认写入服务工作目录下的 `data/photos/{userId}/{year}/{month}`，部署时可用 `PHOTO_STORAGE_ROOT` 指定持久化目录。

### 8.2 查询轮播照片

`GET /api/carousel/photos`

返回当前用户全部未删除照片，按上传时间和主键倒序排列。没有照片时返回空数组。

### 8.3 读取照片内容

`GET /api/carousel/photos/{photoId}/content`

返回 `image/webp` 文件流。服务端会同时校验照片归属和逻辑删除状态，其他用户无法通过照片主键读取文件。

### 8.4 移出轮播

`DELETE /api/carousel/photos/{photoId}`

成功返回 `204 No Content`。该操作写入 `deleted_at`，照片立即退出列表和轮播；当前版本保留物理文件，以便后续增加回收站或恢复能力。

## 9. SSE 实时事件

### 9.1 订阅事件流

`GET /api/events`

该连接用于让网页和 Electron 在小猫资料或提醒变化后及时刷新。它是持续连接，不会像普通 HTTP 请求一样立即结束。

```javascript
const source = new EventSource('http://127.0.0.1:8080/api/events')

source.addEventListener('cat-profile.updated', (event) => {
  const data = JSON.parse(event.data)
  console.log(data.profileId, data.version)
})

source.addEventListener('reminder.changed', (event) => {
  const data = JSON.parse(event.data)
  console.log(data.reminderId, data.version, data.action)
})
```

建立连接后首先收到：

```text
event:connection.ready
data:{"connectedAt":"2026-09-16T08:30:00Z"}
```

小猫名字修改成功后收到：

```text
id:1-1
event:cat-profile.updated
data:{"profileId":1,"version":1}
```

`cat-profile.updated` 只表示资料发生变化。客户端发现事件中的版本更高时，再调用 `GET /api/cat/profile` 获取完整资料。服务端约每 25 秒发送一条 `keepalive` 注释帧维持连接。

提醒创建、修改、完成或删除的事务提交成功后收到：

```text
id:reminder-4-1-COMPLETED
event:reminder.changed
data:{"reminderId":4,"version":1,"action":"COMPLETED"}
```

`action` 可能为 `CREATED`、`UPDATED`、`COMPLETED` 或 `DELETED`。事件只携带变更摘要，客户端收到后重新读取当前提醒列表；重新连接成功后也应主动校准一次，以补偿断线期间未收到的事件。

## 10. 错误响应

所有普通接口使用统一错误结构：

```json
{
  "code": "PERSONAL_RECORD_VERSION_CONFLICT",
  "message": "Personal record has been updated. Refresh and try again.",
  "requestId": "0f54e8f8d6254bb8ad87cc6bf749d6fd",
  "timestamp": "2026-09-16T08:30:00Z",
  "path": "/api/records/1",
  "details": []
}
```

| 字段 | 说明 |
| --- | --- |
| `code` | 稳定的错误码，前端应优先依据它判断错误类型 |
| `message` | 便于开发和排查的英文说明 |
| `requestId` | 本次请求编号，可用于查找同一条后端日志 |
| `timestamp` | 错误发生时间 |
| `path` | 发生错误的请求路径 |
| `details` | 字段级错误列表；当前版本通常为空数组 |

常用错误码：

| HTTP 状态 | 错误码 | 说明 |
| --- | --- | --- |
| `400` | `REQUEST_BODY_REQUIRED` / `REQUEST_BODY_INVALID` | 请求体缺失或 JSON 无法解析 |
| `400` | `PROFILE_ID_REQUIRED` / `PROFILE_ID_INVALID` | 小猫资料主键缺失或不是正整数 |
| `400` | `CAT_NAME_REQUIRED` / `CAT_NAME_TOO_LONG` | 小猫名字为空或超过 20 个字符 |
| `400` | `PROFILE_VERSION_REQUIRED` / `PROFILE_VERSION_INVALID` | 小猫资料版本缺失或为负数 |
| `400` | `RECORD_TYPE_REQUIRED` / `RECORD_TYPE_INVALID` | 文章类型缺失或不在允许范围内 |
| `400` | `RECORD_CONTENT_REQUIRED` / `RECORD_CONTENT_TOO_LONG` | 正文为空或超过 100000 个字符 |
| `400` | `RECORD_TITLE_TOO_LONG` / `RECORD_MOOD_TOO_LONG` | 标题或心情超过长度限制 |
| `400` | `RECORD_DATE_REQUIRED` | 未填写文章日期 |
| `400` | `RECORD_ID_REQUIRED` / `RECORD_ID_INVALID` | 文章主键缺失或格式、范围不正确 |
| `400` | `RECORD_VERSION_REQUIRED` / `RECORD_VERSION_INVALID` | 文章版本缺失或格式、范围不正确 |
| `400` | `PAGE_INVALID` / `PAGE_SIZE_INVALID` / `PAGE_TOO_LARGE` | 分页参数不正确 |
| `400` | `RECALL_LIMIT_INVALID` | 回忆数量不在 1～50 内 |
| `400` | `ACTIVITY_START_DATE_REQUIRED` / `ACTIVITY_END_DATE_REQUIRED` | 写作足迹缺少开始日期或结束日期 |
| `400` | `ACTIVITY_DATE_INVALID` | 写作足迹日期格式不正确 |
| `400` | `ACTIVITY_DATE_RANGE_INVALID` / `ACTIVITY_DATE_RANGE_TOO_LARGE` | 日期顺序错误或查询范围超过 366 天 |
| `400` | `KNOWLEDGE_QUESTION_REQUIRED` / `KNOWLEDGE_QUESTION_TOO_LONG` | 知识检索问题为空或超过 500 个字符 |
| `400` | `EMOTION_CONTENT_REQUIRED` | 每日情绪内容为空 |
| `400` | `EMOTION_DATE_INVALID` | 情绪查询日期格式不正确 |
| `400` | `REMINDER_ID_REQUIRED` / `REMINDER_ID_INVALID` | 提醒主键缺失或格式、范围不正确 |
| `400` | `REMINDER_CONTENT_REQUIRED` / `REMINDER_CONTENT_TOO_LONG` | 提醒内容为空或超过 200 个字符 |
| `400` | `REMINDER_TIME_REQUIRED` | 未填写提醒时间；无法解析的时间文本返回 `REQUEST_BODY_INVALID` |
| `400` | `REMINDER_TIME_IN_PAST` | 创建或修改时提交的提醒时间早于后端当前时间 |
| `400` | `REMINDER_SCOPE_INVALID` | 查询范围不是 `TODAY` 或 `PENDING` |
| `400` | `REMINDER_VERSION_REQUIRED` / `REMINDER_VERSION_INVALID` | 提醒版本缺失或格式、范围不正确 |
| `400` | `REMINDER_NOT_PENDING` | 尝试修改或再次完成已经完成的提醒 |
| `400` | `PHOTO_ID_REQUIRED` / `PHOTO_ID_INVALID` | 照片主键缺失或格式、范围不正确 |
| `400` | `PHOTO_FILE_REQUIRED` / `PHOTO_FILE_TYPE_INVALID` / `PHOTO_FILE_INVALID` | 照片缺失、不是 WebP 或文件内容无效 |
| `400` | `PHOTO_DIMENSIONS_INVALID` | 照片尺寸不是 1200 × 900 |
| `413` | `PHOTO_FILE_TOO_LARGE` | 裁剪后的照片超过 2 MB |
| `404` | `CAT_PROFILE_NOT_FOUND` | 小猫资料不存在 |
| `404` | `PERSONAL_RECORD_NOT_FOUND` | 文章不存在或已逻辑删除 |
| `404` | `REMINDER_NOT_FOUND` | 提醒不存在或已逻辑删除 |
| `404` | `PHOTO_NOT_FOUND` | 照片不存在、已逻辑删除或不属于当前用户 |
| `404` | `RESOURCE_NOT_FOUND` | 请求路径不存在 |
| `409` | `CAT_PROFILE_VERSION_CONFLICT` | 小猫资料版本已变化 |
| `409` | `PERSONAL_RECORD_VERSION_CONFLICT` | 文章版本已变化 |
| `409` | `REMINDER_VERSION_CONFLICT` | 提醒版本已变化 |
| `405` | `METHOD_NOT_ALLOWED` | HTTP 方法不支持 |
| `415` | `MEDIA_TYPE_NOT_SUPPORTED` | 请求体媒体类型不支持，JSON 接口应使用 `application/json` |
| `503` | `KNOWLEDGE_RETRIEVAL_SERVICE_UNAVAILABLE` | Python 知识检索服务未启动或响应异常 |
| `500` | `PERSONAL_RECORD_CREATE_FAILED` | 文章没有成功写入 |
| `500` | `EMOTION_CREATE_FAILED` | 每日情绪没有成功写入 |
| `500` | `REMINDER_CREATE_FAILED` / `REMINDER_UPDATE_FAILED` | 提醒没有成功写入或更新 |
| `500` | `PHOTO_STORAGE_FAILED` / `PHOTO_CREATE_FAILED` / `PHOTO_FILE_UNAVAILABLE` | 照片文件或数据库记录没有成功保存、读取 |
| `500` | `DATABASE_OPERATION_FAILED` | 数据库操作失败 |
| `500` | `INTERNAL_SERVER_ERROR` | 未预期的服务端错误 |

发生 `409` 时不要直接覆盖新数据，应重新查询最新对象，展示或合并改动后，再携带新 `version` 提交。
