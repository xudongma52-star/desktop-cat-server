<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.desktopcat.server.catprofile.CatProfileMapper">
  <select id="selectPrimaryProfile" resultType="com.desktopcat.server.catprofile.CatProfile">
    SELECT profile_id,
           profile_key,
           cat_name,
           version,
           created_at,
           updated_at
    FROM cat_profile
    WHERE profile_key = 'primary'
  </select>

  <update id="updatePrimaryName">
    UPDATE cat_profile
    SET cat_name = #{catName},
        version = version + 1,
        updated_at = CURRENT_TIMESTAMP
    WHERE profile_id = #{profileId}
      AND profile_key = 'primary'
      AND version = #{version}
  </update>
</mapper>
