spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://127.0.0.1:5432/desktop_cat}
    username: ${DB_USERNAME:desktop_cat}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 5

mybatis:
  configuration:
    map-underscore-to-camel-case: true
